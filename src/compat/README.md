# src/compat — upstream compatibility shim (placeholder)

Empty until Milestone 1 vendors upstream OpenFOAM. From then on, SPUME code
reaches upstream APIs **only** through headers in this directory — no direct
`vendor/` includes anywhere else in `src/`.

Milestone 0 is deliberately OpenFOAM-free, so nothing lives here yet.
